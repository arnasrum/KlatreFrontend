
type userStats = {
    year: number,
    month: number,
    day: number,
    routesTried: number,
    totalTries: number,
    totalCompleted: number,
    groupId: number,
    userId: number,
}

export default userStats;